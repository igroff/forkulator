{
  "name": "application",
  "version": "0.0.1",
  "description": "A web application using express and stuff",
  "main": "server.js",
  "scripts": {
    "start": "export APP_NAME=${APP_NAME-$(basename `pwd`)}; test -r ~/.${APP_NAME}.env && . ~/.${APP_NAME}.env ; ./node_modules/.bin/coffee server.coffee",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "3.0.5",
    "body-parser": "1.14.1",
    "coffee-script": "1.8.0",
    "connect": "3.1.1",
    "express": "4.8.6",
    "lodash": "3.10.1",
    "simplog": "0.1.0",
    "through": "2.3.8"
  },
  "devDependencies": {
    "supervisor": "0.6.0"
  },
  "engines": {
    "node": "5.5.0"
  }
}
